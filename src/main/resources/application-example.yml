spring:
  application:
    name: sistema-agendamentos-consultas-medicas

  datasource:
    url: jdbc:postgresql://localhost:5432/seu_database
    username: seu_username
    password: sua_senha
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
      format-sql: true
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect